#include<stdio.h> 

int main(){
    // Size of operator:

    printf("Storage of int in my computer's architecture: %d \n", sizeof(int));
    printf("Storage of float in my computer's architecture: %d \n", sizeof(float));
    printf("Storage of character in my computer's architecture: %d \n", sizeof(char));
    return 0;
}