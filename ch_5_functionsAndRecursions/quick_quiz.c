#include<stdio.h> 
#include<math.h> 

int main(){
    int k, area;
    k=20 ;
    area= pow(k, 2);

    printf("Area of the square is : %d", area);
    printf("Area of the square is : %f", pow(k, 2));
    return 0;
}